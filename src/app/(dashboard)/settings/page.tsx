import { ComingSoon } from '@/components/dashboard/coming-soon';

export default function SettingsPage() {
  return <ComingSoon title="Settings" phase="Phase 7 — Analytics, Audit Log, Notifications, Settings" />;
}
